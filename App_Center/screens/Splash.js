/*import React from "react";
import SplashScreenMask from "react-native-splash-screen-mask";
import { StyleSheet, Text, View } from "react-native";

import IMAGE from "../assets/logo.png";

const Splash = ({ navigation }) => {
  return (
    <View>
      <SplashScreenMask
        imageSource={IMAGE}
        navigationAction={() => navigation.navigate("tutorial")}
        backgroundStyle={styles.backgroundStyle}
        duration={3000}
      />

      <View>
        <Text textStyle={styles.textStyle}>
        
        </Text>
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  backgroundStyle: {
    position: "absolute",
    width: 850,
    height: 850,
    left: -238,
    top: -92,
    backgroundColor: "#EEF5FF",
  },
  textStyle: {
    position: "relative",
    width: 29.95,
    height: 37.07,
    left: 191.46,
    top: 250.39,
    backgroundColor: "#36396C",
  },
});

export default Splash;*/
